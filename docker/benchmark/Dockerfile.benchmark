FROM python:3.12-slim

ARG HOST_UID=1000
ARG HOST_GID=1000

# Install system dependencies and remove the apt package lists afterward.
RUN apt-get update \
    && apt-get install --yes --no-install-recommends \
        git \
    && rm -rf /var/lib/apt/lists/*

# Create a non-root user whose numeric UID/GID match the host user.
RUN groupadd \
        --gid "${HOST_GID}" \
        benchmark \
    && useradd \
        --uid "${HOST_UID}" \
        --gid "${HOST_GID}" \
        --create-home \
        --home-dir /home/benchmark \
        --shell /bin/bash \
        benchmark

ENV WORKSPACE_DIR=/home/benchmark/workspace
ENV HERACLES_BENCHMARK_PATH=/home/benchmark/workspace/benchmark

RUN mkdir -p "${WORKSPACE_DIR}"

# Application sources.
COPY external/heracles/heracles /home/benchmark/workspace/heracles
COPY external/heracles_agents /home/benchmark/workspace/heracles_agents
COPY configs /home/benchmark/workspace/benchmark/configs
COPY data /home/benchmark/workspace/benchmark/data
COPY scripts/benchmark_manifest.py \
     /home/benchmark/workspace/benchmark/scripts/benchmark_manifest.py

RUN pip install --no-cache-dir "${WORKSPACE_DIR}/heracles_agents[all]" \
    && pip install --no-cache-dir --force-reinstall --no-deps \
        "${WORKSPACE_DIR}/heracles"

# Copy the container benchmark entry point into the image.
COPY docker/benchmark/benchmark_runner.sh \
     /home/benchmark/workspace/benchmark_runner.sh

RUN chmod 0755 /home/benchmark/workspace/benchmark_runner.sh \
        /home/benchmark/workspace/benchmark/scripts/benchmark_manifest.py \
    && chown -R benchmark:benchmark \
        /home/benchmark

USER benchmark:benchmark

WORKDIR /home/benchmark/workspace

ENV USER=benchmark
ENV HOME=/home/benchmark

CMD ["bash"]
